PROJECT(SubversionDumper)

FIND_PACKAGE(Qt4 REQUIRED)
FIND_PACKAGE(Boost REQUIRED)

SET(QT_DO_NOT_USE_GUI TRUE)

INCLUDE(${QT_USE_FILE})

ADD_EXECUTABLE(SubversionDumper main.cxx)

TARGET_LINK_LIBRARIES(SubversionDumper ${QT_LIBRARIES})

