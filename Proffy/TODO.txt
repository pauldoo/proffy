Callees should be clumped by symbol+file+line : symbol.

Show time spent executing (excluding callees).

Brand with a version number.

Produce a packaging script.

64-bit builds - (change from IDE to nmake?).

